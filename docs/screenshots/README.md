# Screenshots

Add screenshots here after running the system:
- `chat_interface.png` — The query interface showing retriever badges and CRAG indicators
- `eval_dashboard.png` — The evaluation dashboard with RAGAS metric charts

To capture:
1. Start the system (`docker-compose up -d && uvicorn app.main:app`)
2. Ingest some documents and ask a few queries
3. Take screenshots of the chat and eval tabs
